from .base import (points,
                   scalars_strategies,
                   segments,
                   segments_pairs)
from .planar import (polygons,
                     triangles,
                     triangles_vertices)
