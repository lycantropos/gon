from .base import (points,
                   scalars_strategies)
from .shaped import (polygons,
                     polygons_with_points,
                     segments,
                     segments_pairs,
                     segments_with_points,
                     triangles,
                     triangles_vertices)
