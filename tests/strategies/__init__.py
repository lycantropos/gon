from .base import (points,
                   scalars_strategies,
                   segments,
                   segments_pairs)
from .planar import (polygons,
                     polygons_vertices_counts,
                     triangles,
                     triangles_vertices)
