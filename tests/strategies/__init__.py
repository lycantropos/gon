from .base import (points,
                   scalars_strategies,
                   segments,
                   segments_pairs)
from .shaped import (polygons,
                     triangles,
                     triangles_vertices)
