from .base import (points,
                   scalars_strategies)
from .linear import (segments,
                     segments_pairs,
                     segments_with_points)
from .shaped import (invalid_vertices,
                     polygons,
                     polygons_with_points,
                     triangles,
                     triangles_vertices)
